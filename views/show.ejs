<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Specific Toy</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            color: #333;
        }

        h1 {
            text-align: center;
            background-color: #1d8eff;
            color: white;
            margin: 0;
            padding: 20px 0;
        }

        .form-container {
            max-width: 600px;
            margin: 30px auto;
            background: white;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: bold;
            margin: 10px 0 5px;
        }

        input[type="text"],
        input[type="number"],
        textarea {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        textarea {
            resize: vertical;
        }

        button {
            background-color: #1d8eff;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 20px;
        }

        button:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }

        .view-link {
            display: block;
            text-align: center;
            margin-top: 15px;
            text-decoration: none;
            color: #1d8eff;
            font-weight: bold;
        }

        .view-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>View Specific Toy</h1>
    <div class="form-container">
        <form action="/toys/<%= toy._id %>?_method=PATCH" method="POST" id="editToyForm">
            <label for="identifier">Toy ID:</label>
            <input type="text" name="identifier" id="identifier" disabled value="<%= toy.id %>" style="font-weight: bold;">

            <label for="name">Toy Name:</label>
            <input type="text" name="name" id="name" disabled value="<%= toy.name %>">
            
            <label for="category">Toy Category:</label>
            <input type="text" name="category" id="category" disabled value="<%= toy.category %>">
            
            <label for="quantity">Toy Quantity:</label>
            <input type="number" name="quantity" id="quantity" disabled value="<%= toy.quantity %>">
            
            <label for="price">Toy Price:</label>
            <input type="number" name="price" id="price" disabled value="<%= toy.price %>" >
            
            <label for="description">Toy Description:</label>
            <textarea name="description" id="description" rows="4" disabled><%= toy.description %></textarea>
            
            <a href="/toys" class="view-link">View All Toys</a>
        </form>
    </div>

</body>
</html>
