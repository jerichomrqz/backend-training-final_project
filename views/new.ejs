<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Toy</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            color: #333;
        }

        h1 {
            text-align: center;
            background-color: #1d8eff;
            color: white;
            margin: 0;
            padding: 20px 0;
        }

        .form-container {
            max-width: 600px;
            margin: 30px auto;
            background: white;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: bold;
            margin: 10px 0 5px;
        }

        input[type="text"],
        input[type="number"] {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        input::placeholder {
            color: #aaa;
        }

        button {
            background-color: #1d8eff;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 20px;
        }

        button:hover {
            background-color: #0062e1;
        }

        .view-link {
            display: block;
            text-align: center;
            margin-top: 15px;
            text-decoration: none;
            color: #1d8eff;
            font-weight: bold;
        }

        .view-link:hover {
            text-decoration: underline;
        }

        .required::after{
            content: "*";
            color: red;
            margin-left: 5px;
        }
    </style>
</head>
<body>
    <h1>Add New Toy</h1>
    <div class="form-container">
        <form action="/toys" method="POST">
            <label for="name" class="required">Toy Name:</label>
            <input type="text" placeholder="Enter toy name" name="name" id="name" required>
            
            <label for="category" class="required">Toy Category:</label>
            <input type="text" placeholder="Enter toy category" name="category" id="category" required>
            
            <label for="quantity" class="required">Quantity:</label>
            <input type="number" placeholder="Enter toy quantity" name="quantity" id="quantity" required>
            
            <label for="price" class="required">Price:</label>
            <input type="number" placeholder="Enter toy price" name="price" id="price" required>
            
            <label for="description" class="required">Description:</label>
            <input type="text" placeholder="Enter description" name="description" id="description" required>
            
            <button type="submit">Submit</button>
            <a href="/toys" class="view-link">View All Toys</a>
        </form>
    </div>
</body>
</html>
